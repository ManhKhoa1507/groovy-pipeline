@Library('utils') _

pipeline {
    agent none
    stages {
        stage ('Example') {
            steps {
                script { 
                    log.info 'Starting'
                }
            }
        }
    }
}